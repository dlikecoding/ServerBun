{
  "name": "photos",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server/index.ts",
    "dev": "bun run --watch server/index.ts",
    "startdb": "brew services start postgresql@17",
    "stopdb": "brew services stop postgresql@17",
    "buildS": "bun build server/index.ts --outdir=../serverBunX --target=bun && cp -r ./server/dbms ./server/ai_models ../serverBunX",
    "format": "bun prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "^1.2.8",
    "prettier": "^3.5.3"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@simplewebauthn/server": "^13.1.1",
    "hono": "^4.7.5",
    "mysql2": "^3.12.0",
    "zod": "^3.24.2"
  }
}
