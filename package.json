{
  "name": "photos",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server/index.ts",
    "dev": "bun run --watch server/index.ts",
    "db": "brew services start postgresql@17",
    "sdb": "brew services stop postgresql@17",
    "buildS": "bun build server/index.ts --outdir=../serverBunX --target=bun && cp -r ./models ../serverBunX",
    "format": "bun prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "^1.2.10",
    "prettier": "^3.5.3"
  },
  "peerDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.3",
    "@simplewebauthn/server": "^13.1.1",
    "hono": "^4.7.7",
    "zod": "^3.24.3"
  }
}
